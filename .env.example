# Copy this file to `.env` in the repo root and adjust if needed.
# Tests and Docker Compose read these values (tests load `.env` via python-dotenv).

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=workshop
POSTGRES_PASSWORD=workshop_local_dev
POSTGRES_DB=communitybridge_workshop
